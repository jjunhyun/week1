server {
    listen       80;
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
    }

    # 커스텀 포인트: 상태 확인용 엔드포인트 추가
    location /health {
        return 200 "OK\n";
    }
}
